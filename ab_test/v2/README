Current status here:

An implementation of the (rather simple) object model I (Rik)
am proposing.

This implementation loads backend + filters dynamically (as plugins).

Comments please.

Rik <rik@kde.org>
