if(KDEPIM_ONLY_KLEO AND Boost_TOPOLOGICAL_SORT_DIR)
  add_subdirectory(kleopatra)
else(KDEPIM_ONLY_KLEO AND Boost_TOPOLOGICAL_SORT_DIR)

  if(KDEPIM_BUILD_EVERYTHING)
    add_subdirectory(knode)
  endif(KDEPIM_BUILD_EVERYTHING)

  if(QGPGME_FOUND)
    add_subdirectory(korganizer)
    add_subdirectory(kmail)
    add_subdirectory(kleopatra)
  endif(QGPGME_FOUND)

  if(KPILOT_SUPPORTED)
    add_subdirectory(kpilot)
  endif(KPILOT_SUPPORTED)

  add_subdirectory(akregator)
  add_subdirectory(kabcclient)
  add_subdirectory(kaddressbook)
  add_subdirectory(kalarm)
  add_subdirectory(kioslave)
  add_subdirectory(kjots)
  add_subdirectory(knotes)
  add_subdirectory(konsolekalendar)
  add_subdirectory(kontact)
  add_subdirectory(ktimetracker)
  add_subdirectory(kwatchgnupg)
endif(KDEPIM_ONLY_KLEO AND Boost_TOPOLOGICAL_SORT_DIR)
