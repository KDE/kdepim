if include_exchange_SUBDIR
EXCHANGE_SUBDIR=exchange 
endif

SUBDIRS = lib remote egroupware $(EXCHANGE_SUBDIR) kolab slox birthdays newexchange
# SUBDIRS = remote egroupware $(EXCHANGE_SUBDIR) kolab slox
