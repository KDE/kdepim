# install in data dir because docs are getting removed on maemo automatically by docpurge
install(FILES de/index.html DESTINATION ${DATA_INSTALL_DIR}/kontact-touch/de/manual/)
install(FILES en/index.html DESTINATION ${DATA_INSTALL_DIR}/kontact-touch/en/manual/)
